<?php
// ============================================================
// ШАБЛОН настроек. Скопируйте этот файл в config.php НА ХОСТИНГЕ
// и впишите свои значения. config.php в репозиторий не попадает
// (он в .gitignore), а этот шаблон — без секретов, его можно хранить.
// ============================================================
return [
    // --- Telegram ---
    'bot_token'    => '',   // НОВЫЙ токен от @BotFather (после /revoke)
    'relay_url'    => 'https://telegram-relay.pisyjones.workers.dev',
    'relay_secret' => '',   // НОВЫЙ RELAY_SECRET (тот же, что в Cloudflare)

    // --- Кто получает заявки ---
    'subscribe_password' => '',   // придумайте, только латиница и цифры, например: Studio2026x
    'chat_ids' => [],             // можно сразу вписать id чатов: [123456789, 987654321]

    // --- Защита формы ---
    'form_secret'   => '',        // любая длинная случайная строка (32+ символа)
    'allowed_hosts' => ['studio.dvoretckaya.ru', 'www.studio.dvoretckaya.ru'],
    'studio_phone'  => '+7 (926) 428-11-44',   // показывается, если сработал лимит

    // 'block'   — спам уходит в data/spam.php, в Telegram не приходит
    // 'monitor' — спам приходит в Telegram с пометкой «ПОХОЖЕ НА СПАМ» (для проверки)
    'mode' => 'block',

    'limits' => [
        'per_ip_hour'      => 3,    // заявок с одного адреса в час
        'per_ip_day'       => 8,    // ... и в сутки
        'global_hour'      => 40,   // всего заявок в час
        'min_fill_seconds' => 4,    // быстрее — считается ботом
    ],

    // Шаблоны спама по умолчанию заданы в lib.php. Переопределять не нужно.

    // true — ничего не отправлять в Telegram, только записывать в data/outbox.php (для проверки)
    'dry_run' => false,
];
